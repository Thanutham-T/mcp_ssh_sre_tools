[project]
name = "mcp-ssh-sre-tools"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "bitwarden-sdk>=2.0.0",
    "mcp>=1.26.0",
    "paramiko>=4.0.0",
    "python-dotenv>=1.2.2",
    "pyyaml>=6.0.3",
]
